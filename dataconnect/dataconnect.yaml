specVersion: "v1beta"
serviceId: "quad_count"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "quad_count-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
